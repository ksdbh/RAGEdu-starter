# Docker & Compose

This page will go deeper on running EduRAG using the provided Docker Compose
files, including:

- How the backend, frontend, OpenSearch, and LocalStack containers are wired.
- Example commands for running tests inside the backend container.
- Tips for inspecting logs and health checks in the composed stack.

For now, see the Docker Compose tab in [Getting Started](getting-started.md).
